server:
  port: 8081
spring:
  application:
    name: vendedor-service
  main:
    allow-bean-definition-overriding: true
  datasource:
    url: jdbc:h2:mem:vendedor
    username: sa
    password:
    driver-class-name: org.h2.Driver
  h2:
    console:
      enabled: true
  jpa:
    show-sql: true
    database-platform: org.hibernate.dialect.H2Dialect
    defer-datasource-initialization: true

